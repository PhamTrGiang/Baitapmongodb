<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.3/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
    <title>Document</title>
    <style>
        .page-top {
            background-color: white;
            padding: 5px 15px;
            border-radius: 5px 5px 0px 0px;
            margin: 3px 0px;
        }

        h2 {
            padding: 0px;
            margin: 0px;
        }

        .btn-add {
            border: 0px;
            padding: 5px;
            width: 80px;
            border-radius: 3px;
            background-color: #7bc74d;
            color: white;
            font-weight: bold;
        }

        .btn-add:hover {
            background-color: #90bc75;
        }

        .btn-add:focus {
            background-color: #4f862d;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="page-top row">
            <h2 class="col-md-10">Nhân Viên</h2>
            <div class="col-md-2">
                <form action='/add' method="get">
                    <button type="submit" class="btn-add">Add</button>
                </form>
            </div>
        </div>
        <table class="table">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Name</th>
                    <th>Address</th>
                    <th>Wage</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                {{#each list}}
                <tr>
                    <td>{{_id}}</td>
                    <td>{{ten}}</td>
                    <td>{{diachi}}</td>
                    <td>{{luong}}</td>
                    <td>
                        <a href="/delete/{{_id}}" class="btn btn-default"><i class="glyphicon glyphicon-trash"></i></a>
                        <a href="/edit/{{_id}}" class="btn btn-default"><i class="glyphicon glyphicon-pencil"></i></a>
                    </td>
                </tr>
                {{/each}}
            </tbody>

        </table>
    </div>
</body>

</html>